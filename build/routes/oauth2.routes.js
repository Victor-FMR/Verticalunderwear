"use strict";
// import passport from "passport";
// import { Request, Response, Router } from "express";
// import { isNotAuth2 } from "../utils/oauth2.handles.js";
// const server = Router();
// server.get("/auth/google", isNotAuth2, passport.authenticate("google",{prompt: 'select_account'}));
// server.get('/auth/google/redirect' ,passport.authenticate('google', {failureRedirect: "/Home"}), (req: Request, res: Response)=>{
//     res.redirect('/')
// })
// export default server;
